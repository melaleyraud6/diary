<p> <%= @resource.name%> 様</p>
<p> 登録メールアドレス: <%= @resource.email %></p>

<p>制限回数を超えてしまったためアカウントがロックされました。</p>

<p>アカウントのロック解除をされる場合はお手数ですが下記のリンクからお願いいたします:</p>

<p><%= link_to 'ロック解除', unlock_url(@resource, unlock_token: @token) %></p>
